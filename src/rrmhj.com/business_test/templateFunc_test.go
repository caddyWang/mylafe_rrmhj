package business

import (
	. "rrmhj.com/business"
	"testing"
)

/*
func Test_DefaultHeadImg(t *testing.T) {
	defHeadImg := "img/defuser.png"

	if defHeadImg == "" {
		t.Error("配置文件app.conf中不存在'defheadimg'配置项，或者'defheadimg'配置项内容为空！")
	}

	if val := DefaultHeadImg(""); val != defHeadImg {
		t.Error("未设定用户头像图片时，自动设置默认头像不成功！")
	}

	if val := DefaultHeadImg("    "); val != defHeadImg {
		t.Error("未设定用户头像图片时，自动设置默认头像不成功！")
	}

	if val := DefaultHeadImg("dfwdfwx"); val != defHeadImg {
		t.Error("头像设置不准确时，自动设置默认头像不成功！")
	}

	if val := DefaultHeadImg("head.png"); val != "head.png" {
		t.Error("存在头像时，不做任何处理时，失败！")
	}
}
*/
